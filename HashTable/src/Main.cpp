#include <iostream>

/**
 * For any problem, have hash tables at the top of your mind
 * for solving the problem.
 * 
 * A hash table is a key-value lookups. Gives you a way of, given a key,
 * associating a value with it for very quick lookups.
 */
class HashTable
{

};


int main()
{
	std::cout << "Hash tables!" << std::endl;

	std::cin.get();
}